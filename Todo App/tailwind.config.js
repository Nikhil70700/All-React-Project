/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",                   // Include index.html
    "./src/**/*.{js,ts,jsx,tsx}",     // Include all JS, TS, JSX, TSX files inside the 'src' directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
